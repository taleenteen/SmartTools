# คู่มือการใช้งาน SmartTools

เอกสารนี้สอน **วิธีใช้เว็บ** หลัง deploy แล้ว ไม่ใช่เอกสารพัฒนาโค้ด

ถ้ายังไม่เคยตั้งค่า Cloudflare / KV ให้อ่านหมวด [งานที่ต้องทำก่อนใช้งานเต็มที่](#งานที่ต้องทำก่อนใช้งานเต็มที่) ก่อน

---

## เว็บนี้คืออะไร

SmartTools มี 2 ส่วนหลัก:

1. **หน้าบุ๊กมาร์ก** — รวมลิงก์ที่ใช้อยู่เป็นประจำ จัดเป็นหมวดและการ์ด แก้ได้จากหลังบ้านโดยไม่ต้องแก้โค้ด
2. **หน้าเครื่องมือ** — เครื่องมือเรียน / คำนวณ ที่เปิดใช้ได้ทันที

หลังบ้านอยู่ที่ **Settings** `/settings` (ทางลัด `/c`) ใช้เพิ่มลิงก์ หมวด ผู้ใช้ Inbox และสำรองข้อมูล

`config.html` ยังอยู่ใน deploy เป็นทางสำรองฉุกเฉิน ถ้า Settings มีปัญหา

---

## แผนที่หน้าเว็บ

แทนที่ `https://โดเมนของคุณ` ด้วย URL จริง เช่น `https://xxxx.pages.dev`

| URL | หน้า | ใครใช้ |
|---|---|---|
| `/` หรือ `/l` | บุ๊กมาร์ก (เลือกธีมอัตโนมัติ) | ทุกคน |
| `/t` หรือ `/tool` | รายการเครื่องมือ (Vue hub) | ทุกคน |
| `/tools/ชื่อ.html` | เครื่องมือรายตัว | ทุกคน |
| `/about.html` | คำอธิบายเครื่องมือ | ทุกคน |
| `/settings` หรือ `/c` | Settings (หลังบ้าน Vue) | แอดมิน / ผู้ใช้ที่ล็อกอิน |
| `/config.html` | หลังบ้านเดิม (rollback) | ฉุกเฉินเท่านั้น |
| `/@ชื่อ` หรือ `/u/ชื่อ` | บุ๊กมาร์กสาธารณะของคนนั้น | คนที่เปิดลิงก์สาธารณะไว้ |

ธีมหน้าบุ๊กมาร์ก (สลับได้จากปุ่มมุมหน้า หรือ `?theme=`):

| พารามิเตอร์ | สไตล์ |
|---|---|
| `?theme=nebula` | 🌌 Nebula |
| `?theme=notion` (ค่าเริ่ม) | 📄 Notion |
| `?theme=stripe` | 💎 Stripe |
| `?theme=dark` | 🖤 Framer |
| `?theme=mint` | 🌿 Mintlify |

ตัวอย่าง: `https://โดเมนของคุณ/?theme=stripe`

---

## งานที่ต้องทำก่อนใช้งานเต็มที่

โปรเจกต์นี้บนแดชบอร์ดชุดใหม่เป็น **Worker** ชื่อ `smart-tools` ไม่ใช่ Pages แบบเก่า

**Deploy บน Cloudflare ยังค้างอยู่** — Git build ล้มที่ `compatibility_date` อนาคต (API 10021) รายละเอียดและสิ่งที่แก้ไปแล้วอยู่ใน [DEPLOY.md](./DEPLOY.md) อย่ากด Retry ของรอบที่ fail จนกว่าจะมีแพตช์ใหม่

| ช่อง | ค่า |
|---|---|
| Build command | `bun run pages:build` |
| Deploy command | `npx wrangler deploy` |
| Root directory | ว่าง |

อย่าใช้ `wrangler pages deploy` — จะยิง API ของ Pages แล้วได้ Authentication 10000

Deploy อย่างเดียว **ยังไม่พอ** ถ้ายังไม่ได้ผูก KV ระบบจะแสดงลิงก์จากไฟล์ `data.js` ใน repo ได้ แต่ **เซฟจากหลังบ้านออนไลน์ไม่ได้** (โหมดอ่านอย่างเดียว)

### 1) ตัวแปรสภาพแวดล้อม

Cloudflare Dashboard → โปรเจกต์ Pages → **Settings → Variables and Secrets** (หรือ Environment variables) → **Production**

ใส่ 3 ตัว แล้วติ๊กเป็น Secret:

| ชื่อ | ความหมาย | ตัวอย่าง |
|---|---|---|
| `ADMIN_USER` | ชื่อล็อกอินแอดมิน | `admin` |
| `ADMIN_PASS` | รหัสล็อกอินแอดมิน | รหัสแข็งแรงของตัวเอง |
| `AUTH_SECRET` | กุญแจเซ็นคุกกี้ล็อกอิน ยาวอย่างน้อย 16 ตัว (แนะนำ 32+) | สุ่มด้วยคำสั่งด้านล่าง |

สร้าง `AUTH_SECRET` ใน PowerShell:

```powershell
node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
```

คัดลอกข้อความที่ได้ไปวางทั้งก้อน ห้ามเว้นว่าง และห้ามใช้คำว่า `please-change-this-secret`

### 2) สร้าง KV แล้วผูกกับโปรเจกต์

แดชบอร์ดใหม่ของ Cloudflare **ย้าย KV ออกจากเมนู Workers & Pages แล้ว** ไม่มีปุ่ม KV อยู่ในหน้ารายการ Pages อีกต่อไป

**สร้าง namespace**

1. เปิดหน้า [Workers KV](https://dash.cloudflare.com/?to=/:account/workers/kv/namespaces) โดยตรง  
   หรือในแดชบอร์ดใช้ช่องค้นหาด้านบนพิมพ์ `KV` แล้วเลือก **Workers KV**  
   (แถบซ้ายมักอยู่กลุ่ม **Storage** / **Compute** → **KV**)
2. กด **Create instance** (ชื่อปุ่มใหม่ ไม่ใช่ Create a namespace)
3. ตั้งชื่ออะไรก็ได้ เช่น `smarttools-fav` แล้วกด **Create**

**ผูกเข้าโปรเจกต์ Pages**

1. กลับไป **Workers & Pages** → คลิกโปรเจกต์ SmartTools ของคุณ
2. ไป **Settings → Bindings → Add → KV namespace**
3. กรอกดังนี้:

| Variable name | KV namespace |
|---|---|
| **`FAV_KV`** | เลือก `smarttools-fav` ที่เพิ่งสร้าง |

ชื่อตัวแปรต้องเป็น `FAV_KV` ตรงๆ ตัวพิมพ์ใหญ่ ผิดแล้วเซฟไม่ได้

ไม่ต้องสร้าง key ใน KV เอง ระบบจะเขียนให้ตอนเซฟครั้งแรก

### 3) Deploy ซ้ำ

หลังใส่ตัวแปรและผูก KV แล้ว ไป **Deployments → Retry deployment** ของรอบล่าสุด

ไม่ทำขั้นนี้ค่าใหม่จะยังไม่เข้า Functions

### 4) ล็อกอินครั้งแรก

1. เปิด `https://โดเมนของคุณ/settings`
2. เลือก **Online**
3. ล็อกอินด้วย `ADMIN_USER` / `ADMIN_PASS` (หรือ Users ที่แอดมินสร้าง)
4. แก้การ์ดในแท็บ **Cards** แล้วกด **Save**
5. ถ้าสำเร็จ ข้อมูลจะอยู่ใน KV จากนั้นหน้าแรกจะอ่านจาก KV แทนไฟล์ใน GitHub

ถ้าเซฟแล้วหน้าแรกไม่เปลี่ยน ให้ดูหมวด [ปัญหาที่พบบ่อย](#ปัญหาที่พบบ่อย)

---

## ใช้หน้าบุ๊กมาร์ก

เปิด `/` จะเจอหมวดและการ์ด

### การ์ด 3 แบบ

| แบบ | พฤติกรรมเมื่อคลิก |
|---|---|
| ทั้งการ์ดกระโดด (`simple`) | คลิกตรงไหนก็ไป URL เดียว |
| คำอธิบายคลิกได้ (`desc-clickable`) | หัวข้อไปลิงก์ A, ข้อความคำอธิบายไปลิงก์ B |
| กางเมนูย่อย (`expandable`) | กดแล้วโผล่การ์ดย่อยหลายใบ |

### โน้ต Markdown บนการ์ด

ถ้าการ์ดมีโน้ต คลิกครั้งแรกจะเด้งหน้าต่างพรีวิว คลิกในโน้ตอีกครั้งจึงตามลิงก์

รองรับหัวข้อ ตัวหนา ตัวเอียง รายการ โค้ด ลิงก์ รูป

แอดมินที่ล็อกอินอยู่แก้โน้ตบนหน้าแรกได้เลย แต่ **ยังไม่ลงถาวรจนกด 💾 保存 ในหลังบ้าน**

### หมวดเข้ารหัส

หมวดลับตอนล็อกจะเห็นแค่เม็ดยาเล็กๆ ไม่โชว์ชื่อหมวด

1. กดเม็ดยา
2. ใส่ **รหัสล็อกอิน** (ไม่ใช่รหัสแยก)
3. เนื้อหาถอดรหัสเฉพาะในแท็บนี้
4. ปิดแท็บ หรือกดปุ่มล็อกมุมล่างขวา = ล็อกทันที

ลืมรหัส = กู้ไม่ได้

### สลับธีม

ปุ่มมุมหน้า (เช่น 📄 Notion) เลือกธีมได้ ระบบจำธีมล่าสุดในเบราว์เซอร์เครื่องนั้น

### ปุ่ม ⚙️ 设置

มุมหน้าบุ๊กมาร์ก มีปุ่ม **Settings** ไป `/settings`

---

## ใช้หน้าเครื่องมือ

เปิด `/t` หรือ `/tools`

เครื่องมือที่อยู่ในเว็บนี้จริง:

| ชื่อ | ทำอะไร |
|---|---|
| 估算计算器 | ประมาณค่า บวกลบคูณหาร พร้อมขั้นตอน |
| 诗词九宫格 | เกมตาราง 3×3 ท่องกวีจีน |
| 生理知识点 | โครงความรู้สรีรวิทยา + เคสคลินิก |
| 口算练习 | สร้างโจทย์คณิต ป.3 และช่วงขึ้น ป.4 พร้อมเฉลย ปริ้นได้ |

รายการอื่นในหน้าเดียวกัน (AI 错题本, 密码管理, 影视聚合 ฯลฯ) เป็น **ลิงก์ออกไปบริการของเจ้าของเดิม** ถ้าไม่ได้โฮสต์บริการเหล่านั้นเอง จะเปิดไม่ได้หรือไม่ใช่ของเรา

สลับธีมหน้าเครื่องมือได้จากปุ่มวงกลมมุมขวาบน

---

## ใช้หลังบ้าน (`/settings`)

ทางลัด `/c` เปิดหน้าเดียวกัน ภาษาใน Settings เป็นไทย ประชื่อฟีเจอร์เป็นอังกฤษ

### เลือกโหมดตอน Login

| โหมด | ใช้เมื่อ | ข้อจำกัด |
|---|---|---|
| **Online** | แก้จากเครื่องไหนก็ได้ ข้อมูลอยู่ใน KV | ต้องผูก `FAV_KV` และตั้ง env แล้ว |
| **Local** | แก้ไฟล์ `data.js` บนเครื่อง | ต้อง Chrome/Edge, เปิดผ่าน `http://` ไม่ใช่ `file://` |

หลัง deploy บน Cloudflare ให้ใช้ **Online** เป็นหลัก

### แท็บใน Settings

| แท็บ | ทำอะไร |
|---|---|
| **Cards** | เพิ่ม/แก้การ์ดและหมวด แล้วกด **Save** |
| **Backup** | พรีวิว / Restore / Download / Delete สำเนา |
| **Source** | สลับหน้าแรกให้อ่านจาก **KV** หรือ **static** |
| **Import** | นำเข้า-ส่งออก `data.js` หรือ CSV (ยังไม่ Save จนกว่าจะกด Save) |
| **Account** | เปลี่ยน **Password**, ตั้ง **slug**, ตั้ง **Inbox Policy** |
| **Inbox** | รับ/ส่งการ์ด, แอดมินมี **Push** |
| **Users** | แอดมินเท่านั้น: สร้างผู้ใช้, Reset, Force delete, **Archives** |

`config.html` ยังเปิดได้ถ้าต้องการ rollback

---

## เพิ่มลิงก์ (การ์ด) — ไม่ต้องแก้โค้ด

1. เข้า `/settings` ล็อกอิน **Online**
2. เลือกแท็บหมวดด้านบน
3. กด **➕ 新增卡片**
4. กรอกอย่างน้อย:
   - **标题** — ชื่อที่เห็นบนการ์ด
   - **图标** — emoji, URL รูป, หรือ SVG
   - **卡片类型** — ดูตารางด้านล่าง
   - **主网址 URL** — ลิงก์หลัก (เว้นว่างได้)
5. กด **确定**
6. **กด 💾 保存 ที่แถบบน** — ขั้นนี้สำคัญ ถ้าไม่กด ของใหม่ยังไม่อยู่บนเซิร์ฟเวอร์

### เลือกประเภทการ์ดยังไง

- ลิงก์เดียว → `simple`
- หัวข้อไปเว็บหลัก คำอธิบายไปเว็บสำรอง → `desc-clickable` แล้วกรอก `descClickable` + `descUrl`
- กลุ่มลิงก์หลายอัน → `expandable` แล้วกด **➕ 添加子卡片**

### การ์ดย่อย

มี 2 สไตล์:

- **两行式** — ไอคอน + ชื่อ + คำอธิบาย
- **紧凑式** — ไอคอน + ข้อความบรรทัดเดียว

ติ๊ก **本地跳转** เมื่อลิงก์เป็นหน้าในไซต์นี้เอง เช่น `tools/ydyjsq.html`

ลากแฮนด์ `⋮⋮` เพื่อเรียงลำดับ  
ย้าย/คัดลอกข้ามหมวดได้จากปุ่มบนการ์ด  
การ์ดย่อยกด ➡ เพื่อย้ายไปการ์ดอื่น (ถ้าย้ายไปการ์ดที่ไม่ใช่ expandable ระบบจะอัปเกรดให้เอง)

---

## เพิ่ม / แก้หมวด

1. เปิดแท็บจัดการหมวด (**大类管理**)
2. กด **➕ 新增大类**
3. ตั้งชื่อ เช่น `🎵 เพลง`
4. (ไม่บังคับ) ใส่ **anchor** เช่น `music` จะได้ลิงก์ `#music` กระโดดตรงหมวดนั้น
5. ติ๊ก **启用展开/折叠** ถ้าอยากให้มือถือพับการ์ดเหลือไม่กี่ใบแรก
6. ติ๊ก **🔐 加密存储** ถ้าเป็นของลับ
7. ปิดโมดัล แล้วกด **💾 保存**

หมวดในตัว 6 อันลบไม่ได้ (☁️ 在线U盘, 📚 授课资料, 🖥️ 网络资源, 🎬 视频聚合, 📨 联系方式, 📨 其他联系方式)

ลบหมวดที่สร้างเอง: การ์ดจะย้ายไป「📦 未分类」ไม่หาย  
ถ้าลบ「未分类」อีกที การ์ดในนั้นจะหายถาวร

หมวดที่สร้างเองจะแสดงระหว่าง「视频聚合」กับ「邮箱」

### เปิดหมวดเข้ารหัส

1. ติ๊ก 🔐 ตอนสร้างหรือแก้หมวด
2. กุญแจคือ **รหัสล็อกอินปัจจุบัน**
3. เซิร์ฟเวอร์เก็บแต่ข้อความเข้ารหัส
4. **ถ้าเปลี่ยนรหัสผ่าน ต้องกด 💾 保存 ทันที** เพื่อเข้ารหัสใหม่ด้วยรหัสใหม่ ไม่งั้นหมวดลับเปิดไม่ได้อีก

อย่าใส่รหัสผ่าน / token ในโน้ตของหมวดธรรมดา — หมวดธรรมดาเก็บข้อความชัด

---

## นำเข้า / ส่งออกข้อมูลจำนวนมาก

กด **📦 导入/导出**

- ส่งออกทั้งก้อนเป็นไฟล์ `data.js`
- หรือใช้ตาราง **XLSX (แนะนำ)** / CSV แล้วแก้ใน Excel / WPS ก่อนนำกลับ

โหมดนำเข้าตาราง:

- **追加** — ถือว่าเป็นการ์ดใหม่ทั้งหมด ต่อท้ายหมวด
- **覆盖** — จับคู่ด้วย `card_id` ที่มีอยู่แล้วแล้วแทนที่ ของที่ไม่มีในไฟล์ยังอยู่

หลังนำเข้า **ยังต้องกด 💾 保存 อีกรอบ**

ถ้ามี emoji ในช่องไอคอน อย่าเซฟเป็น CSV จาก Excel แบบ GBK — ใช้ XLSX

---

## สลับแหล่งข้อมูลหน้าแรก

ปุ่ม **📡 数据源**

| ค่า | หน้าแรกอ่านจาก | ใช้เมื่อ |
|---|---|---|
| KV | สิ่งที่เพิ่งเซฟในหลังบ้าน | ใช้ทุกวัน แนะนำ |
| 静态 (static) | ไฟล์ `data.js` ใน GitHub | rollback / โหมดอ่านอย่างเดียว |

สลับเป็น static แล้วข้อมูลใน KV ไม่หาย แค่หน้าแรกไม่ใช้ ตัดกลับ KV ได้ทันที

---

## ผู้ใช้หลายคน

เฉพาะแอดมิน เปิดแท็บ **Users**

1. **New user** ใส่ Username + Password
2. User ล็อกอินที่ `/settings` จะได้บุ๊กมาร์กว่างของตัวเอง ไม่ปนกับของแอดมิน
3. **Reset** / **Delete** / **Force** ได้จากแถวผู้ใช้
4. ลบแบบ Force จะเก็บ **Archives** ไว้ก่อน (ต้อง Download JSON ก่อน Cleanup)

### เปิดลิงก์สาธารณะ

แท็บ **Account** (ของตัวเอง) หรือปุ่ม **slug** ใน Users (แอดมินตั้งให้คนอื่น)

- ตั้ง slug เช่น `mybookmarks`
- ข้อจำกัด: ตัวอักษรอังกฤษตัวเล็ก / ตัวเลข / `_` / `-`, user ต้องยาว 3–32 ตัว, ห้ามเป็นตัวเลขล้วน, ห้ามคำสงวนอย่าง `admin`, `api`, `config`
- เปิดสวิตช์
- คนอื่นเปิด `https://โดเมน/@mybookmarks` ได้โดยไม่ต้องล็อกอิน
- ใส่ `?theme=stripe` เพื่อบังคับธีม

### ส่งการ์ดให้คนอื่น

- **Push** (แอดมิน ใน Inbox) — เลือกคนรับ + หมวดปลายทาง + การ์ด  
  - **append**: ไปเข้า Inbox ให้เขายอมรับเอง  
  - **force**: ใส่ลงบุ๊กมาร์กเขาเลย (ห้ามหมวด Encrypt)
- **Send** — ทุกคนที่ล็อกอินส่งหากันได้
- ผู้รับเปิด **Inbox** แล้ว **Accept** เข้าหมวดที่ต้องการ
- **Inbox Policy** ใน Account ปิดรับได้

---

## เพิ่มเครื่องมือในหน้า `/t`

รายการเครื่องมืออยู่ที่ Vue hub `/t` (`web/src/views/ToolsView.vue`) ไฟล์เครื่องคิดเลขแต่ละตัวยังเป็น `tools/*.html`

ตัวอย่างการ์ดเครื่องมือเดี่ยว:

```html
<a href="tools/ชื่อไฟล์.html" class="tool-card">
    <span class="tool-icon">🧮</span>
    <h3 class="tool-title">ชื่อเครื่องมือ</h3>
    <p class="tool-desc">คำอธิบายสั้นๆ</p>
</a>
```

ถ้าเครื่องมือเป็นหน้าใน repo ให้วางไฟล์ที่ `tools/ชื่อ.html`  
ถ้าอยากให้โผล่ในบุ๊กมาร์กด้วย ให้เพิ่มการ์ดในหลังบ้าน ติ๊ก **本地跳转** แล้วใส่ URL เป็น `tools/ชื่อ.html`

อย่าลืมอัปเดต `about.html` ถ้าอยากให้หน้า简介ตรงกับรายการจริง

---

## โหมดท้องถิ่น (แก้บนเครื่องโดยไม่ผ่าน KV)

ใช้ตอนยังไม่ผูก KV หรืออยากแก้ไฟล์ใน repo โดยตรง

1. เปิดโฟลเดอร์ด้วยเซิร์ฟเวอร์ท้องถิ่น เช่น

   ```powershell
   python -m http.server
   ```

2. เปิด `http://localhost:5173/settings` (หรือ `bun run --cwd web dev`) ด้วย Chrome หรือ Edge
3. เลือก **Local**
4. ตั้ง Username / Password ครั้งแรก (เก็บเฉพาะเบราว์เซอร์นี้)
5. กด **Connect** เลือกโฟลเดอร์ repo
6. แก้แล้วกด **Save** จะเขียน `data.js` บนดิสก์

อย่าเปิดด้วย `file://` — File System Access API ใช้ไม่ได้

---

## ปัญหาที่พบบ่อย

**เซฟแล้วหน้าแรกไม่เปลี่ยน**  
ตรวจว่า binding ชื่อ `FAV_KV` ตรง และในหลังบ้านแหล่งข้อมูลเป็น KV ไม่ใช่ 静态

**ล็อกอินแล้วขึ้นว่ายังไม่ได้ตั้ง ADMIN_USER / ADMIN_PASS**  
ตัวแปรต้องอยู่ใน Production แล้วต้อง Retry deployment

**ล็อกอินได้ แต่รีเฟรชแล้วหลุด**  
`AUTH_SECRET` เปลี่ยนระหว่างทาง หรือยังไม่ได้ deploy หลังแก้ env

**ล็อกอินผิดหลายครั้งแล้วเข้าไม่ได้**  
ระบบล็อก IP ชั่วคราวหลังผิด 5 ครั้งใน 10 นาที รอแล้วลองใหม่

**หมวดเข้ารหัสเปิดไม่ได้หลังเปลี่ยนรหัส**  
ลืมเซฟหลังเปลี่ยนรหัส ข้อมูลเก่าถูกเข้ารหัสด้วยรหัสเดิม กู้ไม่ได้

**หน้าเครื่องมือมีลิงก์ที่เปิดไม่ได้**  
ลิงก์เหล่านั้นชี้บริการของเจ้าของเดิม ต้องเปลี่ยน URL ใน `web/src/views/ToolsView.vue` หรือลบออก

**อยากดูข้อมูลที่กำลังใช้จริง**  
เปิด `/api/data?format=json`

---

## ทางลัดที่ควรจำ

```
/                 หน้าบุ๊กมาร์ก
/t                หน้าเครื่องมือ
/settings         หลังบ้าน
/c                ทางลัด Settings
/@slug            บุ๊กมาร์กสาธารณะ
?theme=notion     บังคับธีม
#anchor           กระโดดหมวด (ถ้าตั้ง anchor ไว้)
```

กฎสั้นๆ: **เพิ่มลิงก์ที่ Settings แล้วต้องกด Save**  
**เพิ่มเครื่องมือรายตัวที่ `tools/*.html` และรายการที่ `web/src/views/ToolsView.vue` แล้ว deploy ใหม่**
